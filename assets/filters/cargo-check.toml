[filters.cargo-check]
description = "Compact cargo check and clippy outputs"
match_command = "^cargo\\s+(check|clippy)\\b"
strip_ansi = true
strip_lines_matching = [
  "^\\s*$",
  "^\\s*Compiling\\s+",
  "^\\s*Finished\\s+",
  "^\\s*Checking\\s+"
]
keep_lines_matching = [
  "^warning",
  "^error",
  "^\\s*-->"
]
max_lines = 60
on_empty = "cargo check/clippy: ok"
