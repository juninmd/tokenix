[filters.helm-install]
description = "Compact helm install/upgrade output"
match_command = "^helm\\s+(install|upgrade|template)\\b"
passthrough_when_emptied = true
strip_ansi = true
strip_lines_matching = [
  "^\\s*$",
  "^NAME:",
  "^LAST DEPLOYED:",
  "^NAMESPACE:",
  "^REVISION:",
  "^TEST SUITE:",
  "^\\s*\\d+\\.\\d+s$"
]
keep_lines_matching = [
  "^STATUS:",
  "Error:",
  "FAILED",
  "failed to",
  "cannot",
  "timed out",
  "release.*failed"
]
max_lines = 40

[[tests.helm-install]]
name = "clean install"
input = """
NAME: my-release
LAST DEPLOYED: Mon Jan 15 10:30:00 2024
NAMESPACE: default
STATUS: deployed
REVISION: 1
TEST SUITE: None
"""
expected = "STATUS: deployed"

[[tests.helm-install]]
name = "failure shown"
input = """
Error: INSTALLATION FAILED: cannot re-use a name that is still in use
"""
expected = """
Error: INSTALLATION FAILED: cannot re-use a name that is still in use"""