[filters.docker-compose-up]
description = "Compact docker compose up output"
match_command = "^docker\\s+compose\\s+up\\b"
passthrough_when_emptied = true
strip_ansi = true
strip_lines_matching = [
  "^\\s*$",
  "^\\s*\\d+\\.\\d+s",
  "^\\s*\\d+/\\d+",
  "^\\s*=>",
  "^\\s*CACHED",
  "^\\s*DONE",
  "^\\s*CREATED",
  "^\\s*STARTED",
  "^\\s*HEALTHY",
  "^\\s*UNHEALTHY"
]
keep_lines_matching = [
  "error:",
  "failed to",
  "ERROR:",
  "unhealthy",
  "exited",
  "killed"
]
max_lines = 40

[[tests.docker-compose-up]]
name = "failure shown"
input = """
container web-1 exited with code 1
"""
expected = "container web-1 exited with code 1"

[[tests.docker-compose-up]]
name = "silent success stays empty"
input = ""
expected = ""