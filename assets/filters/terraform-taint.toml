[filters.terraform-taint]
description = "Compact terraform taint output"
match_command = "^terraform\\s+taint\\b"
passthrough_when_emptied = true
strip_ansi = true
strip_lines_matching = [
  "^\\s*$",
  "^Resource instance"
]
keep_lines_matching = [
  "Error:",
  "Error:",
  "marked as tainted",
  "No such resource"
]
max_lines = 20


